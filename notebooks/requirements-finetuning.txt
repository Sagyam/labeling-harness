# Environment for notebooks/03-finetuning.ipynb OFF Kaggle -- a SMOKE_TEST on a workstation, or
# a CPU rehearsal of the TPU code path. On a Kaggle TPU the notebook installs what it needs
# itself (its section 2), so nothing here is required there.
#
#   uv venv --python 3.12 notebooks/.venv-ft
#   VIRTUAL_ENV=notebooks/.venv-ft uv pip install -r notebooks/requirements-finetuning.txt \
#       --extra-index-url https://download.pytorch.org/whl/cpu --index-strategy unsafe-best-match
#   notebooks/.venv-ft/bin/python -m ipykernel install --user \
#       --name labeling-harness-ft --display-name "labeling-harness (fine-tuning)"
#
# Python 3.12 is what torch_xla 2.8 was tested on. With torch_xla installed the notebook takes the
# TPU code path, so on a workstation either set FT_DISABLE_XLA=1, or rehearse the TPU path on
# 8 virtual CPU devices with PJRT_DEVICE=CPU CPU_NUM_DEVICES=8. Either way set FT_SMOKE_TEST=1:
# the real model needs ~9 GB for weights and optimizer state before a single activation.
torch==2.8.0
torch_xla==2.8.0
transformers==4.57.1
soundfile==0.14.0
rapidfuzz==3.14.6
numpy==2.5.3
pandas==3.0.5
matplotlib==3.11.1
PyYAML==6.0.3
ipykernel==7.3.0
jupyterlab==4.6.3
