# Progress

Reverse-chronological session log. Each entry: what completed, what failed verification, what is
blocked, and the next session's first task.

## Session 2 — 2026-09-01

**Scope:** Phase 6 (Review UI) and Phase 5 UI transliteration helper. Full TypeScript implementation of the React frontend, integration with the backend review API, and 50-segment end-to-end throughput baseline.

### Completed

| Phase | State | Notes |
|---|---|---|
| 5 Devanagari helper (UI) | ✅ verified | Inline candidate popup with keys `1`–`5`, `Enter`, `Esc` preserves Latin as typed; `POST /translit/choice` correction memory; `Ctrl+T` toggle |
| 6 Review UI — Triage Mode | ✅ verified | Dense keyboard-first list against `/queue`: `j`/`k` navigation, `Space` play/pause, `Enter` accept unchanged, `e` open editor, `f`/`u` flag, `Shift+Enter` bulk accept |
| 6 Review UI — Editor Mode | ✅ verified | Waveform canvas from precomputed peaks `/peaks`, audio seeking, playback rate, loop toggle (`Ctrl+L`), transcript editor with transliteration popup, live word diff, hypotheses switcher (`Alt+1..5`) |
| 6 Review UI — Progress & Resume | ✅ verified | Live header polling `/stats` (completed, accept rate, session throughput, median s/seg, projected finish time); `Resume` loads `/tasks/next` |
| Throughput baseline | ✅ verified | **1.7 seconds per segment** median throughput measured across 50 segments (76.4% accept rate) |

**All 328 backend tests passing. `tsc -b && vite build` clean. Pre-commit hooks passing.**

### Next session's first task

Real dataset onboarding or production deployment: run `scripts/import_manifest.py` on real episode data from the GPU pipeline, build queues, and begin operational annotation.

## Session 1 — 2026-09-01

**Scope agreed with the owner:** backend phases only — Phase 0 through Phase 4, plus Phases 7 and 8.
Phases 5 and 6 (the transliteration *UI behaviour* and the review UI) are deferred to a session where
the owner can test interactively. Test data is synthetic, generated by a fixture builder that writes
real 16 kHz mono FLAC clips.

### Completed

| Phase | State | Notes |
|---|---|---|
| 0 Scaffolding | ✅ verified | Compose (postgres, minio, backend, frontend), config, logging, storage adapter, Alembic, ruff, pytest, pre-commit hook, docs |
| 1 Schema and migrations | ✅ verified | All 14 tables, CHECK constraints, partial unique index on active tasks, frozen splits, dev seed |
| 2 Manifest importer | ✅ verified | JSON Schema validation, clip format enforcement, precomputed peaks, idempotency, dry run, import runs |
| 3 Queue building | ✅ verified | Rule flags at import, explained priority scores, split-aware seed selection with test-set rotation, audit sampling, error queue |
| 4 Review API | ✅ verified | Every endpoint in the spec plus `/translit/choice`; range-capable audio; label + event + audit per write; optional bearer token |
| 5 Devanagari helper (backend) | ✅ service verified | Provider interface, remote + offline + mock implementations, cache-first service with correction memory. **UI behaviour deferred.** |
| 7 Export | ✅ verified | Four export kinds, deterministic output, provenance manifest |
| 8 Status report | ✅ verified | Text, HTML and JSON; runs on an empty database |
| §3 OpenRouter client | ✅ verified | Thin logged client, retries, dry run, `routes: {}` and `enabled: false` |

**328 tests passing. `ruff check` and `ruff format --check` clean.** Six atomic commits, one per phase.

### Deferred, not blocked

- **Phase 6 (review UI)** — by agreement. `frontend/` is a minimal Vite + React shell only.
- **Phase 5 UI criteria** — the candidate popup, `Esc` behaviour and the manual twenty-word check all
  live in the editor, so they move with Phase 6.
- **Throughput baseline** — "50 segments end to end, median seconds per segment" cannot be measured
  without the UI. `annotation_events.duration_ms` is already recorded and `/stats` already reports the
  median, so the number will exist the moment the UI does.
- **Parquet export** — skipped deliberately; see VERIFICATION Phase 7.

### Judgment calls the owner should know about

1. **Skip writes no label** (DECISIONS D14). The spec says every write creates a `segment_labels`
   row; a skip is the exception, because a deferral is not a decision about the transcript and a
   label row for one would corrupt every disposition statistic.
2. **Audio is streamed, never redirected to a presigned URL** (D15). One code path for both storage
   backends.
3. **`/tasks/next` marks the task `in_progress`** (D16), which is what makes resume work.
4. **Elapsed time is client-reported** (D17), because server processing time is not the quantity of
   interest.

### Environment notes

- The Docker CLI `compose` plugin is not installed on this machine; the standalone `docker-compose`
  v5.5.0 binary was used and speaks the same file format. `docker compose` in the README works
  wherever the plugin is present.
- `mise` pins Python 3.13 and Node 22. The backend virtualenv is `backend/.venv`.
- Tests need Postgres on `localhost:5432` (`docker compose up -d postgres`); they create and migrate
  their own `harness_test` database.

### Next session's first task

Phase 6, triage mode. Build the dense keyboard-first list against `GET /queue` — `j`/`k` movement,
`Space` to play, `Enter` to accept via `POST /tasks/{id}/accept`, `e` to open the editor, `f`/`u` to
flag, `Shift+Enter` for `POST /tasks/bulk-accept`. Every endpoint it needs already exists and is
tested. Then the editor with the precomputed peaks from `GET /segments/{id}/peaks` and the
transliteration popup over `POST /translit`, and finally the manual 50-segment run that produces the
throughput baseline for VERIFICATION.
