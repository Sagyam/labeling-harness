__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
node_modules/
dist/
*.tsbuildinfo
data/
exports/
.mise.local.toml
.playwright-mcp/

# diagram build font cache
docs/diagrams/.fontcache/

# Forced-alignment acoustic model: ~300 MB, built by scripts/export_aligner_onnx.py.
# silero_vad.onnx is committed because it is 2.3 MB; this one cannot be.
backend/app/services/models/mms_fa.onnx
backend/app/services/models/mms_fa_vocab.json

# Same artefact when HARNESS_ALIGNER_MODEL_DIR points at the mounted volume (the container does).
data/models/

# Ground-truth source audio and A/B renders kept for audio debugging (D39)
original.mp3
reports/audio-ab/

# Pre-wipe database and object-storage snapshots
backups/

# Jupyter autosaves
.ipynb_checkpoints/

# EDA notebook: the ECAPA embedding and pitch cache, regenerated from the audio in ~20 min on a
# 16-thread CPU.
notebooks/.cache-diar/
# Fine-tuning notebook: its own venv. Its outputs go to exports/finetune/, ignored above.
notebooks/.venv-ft/
